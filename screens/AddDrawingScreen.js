import MainLayout from "../layouts/MainLayout";
import { Text } from "react-native";

const AddDrawingScreen = ({navigation}) => {
    return(
        <MainLayout>
            <Text>Add</Text>
        </MainLayout>
    )
}

export default AddDrawingScreen;
